export interface Passenger {
    _id: String
    name: String
    trips: Number
}

export interface Airline {
    _id: string
    name: string
    country: string
    logo: string
    slogan: string
    head_quaters: string
    website: string
    established: string
  }

  export interface MessageState {
    message: string
  }